[package]
name = "game"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
bevy_app = "0.15"
bevy_ecs = "0.15"
game-api = { path = "../game_api" }

[target.'cfg(target_arch = "wasm32")'.dependencies]
web-time = "1.1.0"
